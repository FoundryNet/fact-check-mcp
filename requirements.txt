fastmcp>=2.0
httpx>=0.27
# Pure HTTP: general web search (Brave Search API), FoundryNet Data Network sibling
# MCP servers (JSON-RPC over /mcp), and RDAP domain-age lookups — all via httpx.
# x402 gating is plain Solana JSON-RPC over httpx — no x402[svm]/solders extra.
